# 한글 글쓰기(윤문) 로드맵

v10은 새 글 작성, 기존 원고 윤문, 번역문 후편집을 **위치 기반 진단·작성 의도 인계·정밀 보호 검증**으로 연결한다. 자동 저자 판정의 범위를 넓히지 않고 사람 검토와 재현 가능한 품질 증적을 강화한다.

## 0. v10에서 완료한 것

- 프로젝트와 공개 저장소를 `neo-korean-writing`로 전환하고, 서비스 이름을 **한글 글쓰기(윤문)**로 통일
- 새 글 작성 경로를 `korean-writing/`에 통합하고, 루트 윤문 스킬에서 글쓰기 요청을 연결
- 통합 진단·보존 도구를 `scripts/korean_writing.py`로 정리
- Cowork 패키지, 연구 산출물, 문서, 테스트, 자동화 경로를 새 구조로 전환
- 실제 span·행·문단·유지 조건을 제공하는 Detect 2.0
- context와 voice 분리, 선택형 윤기자 voice
- Writer–Editor JSON 인계 계약과 검증 명령
- 코드·YAML·표·URL·각주 보호 및 잔존 P0/P1 증가 차단
- 구조 다양성 감사, 장르별 오탐 fixture, 선택형 Kiwi 형태소 계층
- 참조·버전·내장 자산 drift 검사, self-scan, 재현 가능한 `.skill` 빌드

## 1. 말뭉치 기반 보정

Sunny-7 기준값과 문체 진단 등급 경계는 현재 소규모 관찰 기반의 보조 신호다. 칼럼, 기사, 공문서, 기술 문서, SNS의 실제 원고를 장르별로 수집하고, 사람 편집자가 표시한 수정 후보·오탐 사례·윤문 전후의 의미 보존 평가를 통해 보정한다.

측정 지표는 탐지 정밀도·재현율, 사실 보존율, 불필요한 변경률, 장르 적합도, 작성자 문체 유사도, 사람 편집자 선호도를 함께 사용한다.

## 2. 글쓰기와 윤문의 연결 증적 — v10 완료

주제·메모에서 시작한 글이 구조 설계, 초안, 윤문, 최종 검증을 거치는 동안 어떤 판단이 이루어졌는지 기록하는 경량 작업 로그를 추가한다. 독자·목적·채널·근거·LOCK 항목·수정 이유를 분리해 기록해, 새 글 작성과 기존 원고 편집이 같은 품질 기준을 공유하도록 한다.

## 3. 장문과 구조 기반 편집

8,000자 단일 기준 대신 제목, 문단, 표, 코드 블록, 인용, 각주를 인식하는 구조 기반 분할을 도입한다. 장문에서는 섹션별 용어 일관성, 논지 흐름, 중복 설명, 결론의 과장 여부를 별도 점검하되, 원문 구조를 임의로 재편하지 않는다.

## 4. 선택형 한국어 분석 계층 — v10 1단계 완료

기본판은 표준 라이브러리만 유지한다. `kiwipiepy`가 있으면 형태소·종결·조사 분포를 보조 출력하고, 없으면 명시적으로 graceful fallback한다. 다음 단계에서는 장르별 편집자 라벨로 형태소 신호의 정밀도와 오탐을 보정한다.

## 5. 배포와 협업 품질

Windows, macOS, Linux 설치 안내와 에이전트별 패키지 설명을 정리하고, 릴리스마다 `dist/` 스킬 패키지와 테스트 결과를 자동 생성·검증하는 CI를 추가한다. 공개 저장소에서는 기여 가이드, 보안 정책, 릴리스 노트를 단계적으로 갖춰 사용자가 개선 이력을 추적할 수 있게 한다.
