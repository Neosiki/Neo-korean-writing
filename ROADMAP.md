# ROADMAP: korean-humanize

5차 고도화(규칙·구현·평가 일치) 이후의 중장기 방향. 우선순위 순이다.

## 1. 말뭉치 기반 보정

Sunny-7 기준값과 AI 흔적 지수 등급 경계는 현재 소규모 관찰 기반 임시값이다. 다음 평가 세트를 구축해 보정한다.

- 칼럼, 기사, 공문서, 기술문서, SNS 각 100편 이상
- 사람이 직접 표시한 AI 문체 패턴 정답 데이터
- 정상 표현을 포함한 오탐 방지(negative) 데이터
- 윤문 전후의 의미 보존 평가와 편집자 2인 이상의 자연스러움 점수

측정 지표: 탐지 정밀도/재현율, 의미 보존율, 불필요한 변경률, 장르 적합도, 작성자 문체 유사도, 사람 편집자 선호도.

## 2. 장문 분할 개선

8,000자 고정 기준 대신 제목, 문단, 표, 코드 블록을 인식하는 구조 기반 분할로 바꾼다.

## 3. 형태소 분석 선택 의존성

기본판은 표준 라이브러리를 유지하고, kiwipiepy 등 형태소 분석기가 설치된 환경에서는 조사·어미 단위의 정밀 탐지로 자동 승급하는 이중 구조를 만든다.

## 4. write-content 분리

write-content를 별도 저장소 또는 선택형 패키지로 분리하고, 두 스킬이 공유하는 LOCK·정량 도구를 공통 모듈로 뽑는다.

## 5. 배포 체계

Windows, macOS, Linux 설치 문서와 공식 플러그인 manifest를 추가하고, 릴리스마다 dist/의 .skill 파일을 자동 갱신하는 CI 단계를 만든다.
